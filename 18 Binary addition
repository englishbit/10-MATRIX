#include<stdio.h>
int main(){

    int n;
    scanf("%d",&n);
    int num1[n];
    int num2[n];
    int add_res[n+1];
    

    for(int i=0;i<n;i++){
        scanf("%d",&num1[i]);
        
    }
    for(int i=0;i<n;i++){
        scanf("%d",&num2[i]);
    }


    
    return 0;
}